From 89712513eebb0515be2317f9b7c250f864eb8342 Mon Sep 17 00:00:00 2001
From: Ren Wang <iamwrm@gmail.com>
Date: Sun, 19 Jul 2026 11:25:13 +0800
Subject: [PATCH 11/15] Document Anthropic Claude OAuth and model catalog

Document grok anthropic login, status, and logout, the paste-the-code
fallback, credential isolation, the zero-config anthropic/* catalog, and
anthropic/model:effort selection. Clarify API-key Messages support versus
Claude Pro/Max OAuth and the anthropic_oauth advanced custom-entry
override.
---
 .../docs/user-guide/02-authentication.md      | 42 +++++++++++++++++++
 .../docs/user-guide/04-slash-commands.md      | 10 +++--
 .../docs/user-guide/11-custom-models.md       | 28 ++++++++++++-
 3 files changed, 74 insertions(+), 6 deletions(-)

diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/02-authentication.md b/crates/codegen/xai-grok-pager/docs/user-guide/02-authentication.md
index eeda4fb..79f3083 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/02-authentication.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/02-authentication.md
@@ -84,6 +84,48 @@ session.
 
 ---
 
+## Anthropic Claude with a Pro/Max plan
+
+Use Anthropic Claude models through a Claude Pro/Max subscription without API
+keys or `config.toml` edits.
+
+Sign in once:
+
+```bash
+grok anthropic login
+```
+
+The browser flow uses Anthropic's PKCE authorization flow and a fixed callback
+at `http://localhost:53692/callback`. When the browser runs on another
+machine, paste the redirect URL or the `code#state` pair shown by claude.ai
+into the terminal. Credentials are stored separately from the xAI session in
+`~/.grok/anthropic_auth.json`, refreshed automatically, and sent only to the
+Anthropic API.
+
+After login, the `anthropic/*` models appear in the Ctrl+M picker. Switch
+inside the app with a pi-style provider/model reference and optional thinking
+level:
+
+```
+/model anthropic/claude-opus-4-8:high
+/model anthropic/claude-sonnet-5:max
+```
+
+The equivalent CLI form is:
+
+```bash
+grok -m anthropic/claude-opus-4-8:high -p "hello"
+```
+
+Claude models offer thinking levels `low` through `max`; `xhigh` is accepted
+as an alias for `max`.
+
+Use `grok anthropic status` to inspect the credential and
+`grok anthropic logout` to remove it. Anthropic login does not replace or
+modify the normal `grok login` session.
+
+---
+
 ## API Key
 
 For CI/CD, automation, or environments without browser access, use an API key from [console.x.ai](https://console.x.ai):
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/04-slash-commands.md b/crates/codegen/xai-grok-pager/docs/user-guide/04-slash-commands.md
index ea5d78e..39e3b3f 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/04-slash-commands.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/04-slash-commands.md
@@ -140,16 +140,18 @@ colon suffix or a second argument:
 /model Grok Build
 /model Reasoning X high
 /model openai-codex/gpt-5.5:xhigh
+/model anthropic/claude-opus-4-8:high
 ```
 
-Provider-prefixed IDs such as `openai-codex/gpt-5.5` explicitly select the
+Provider-prefixed IDs such as `openai-codex/gpt-5.5` or
+`anthropic/claude-opus-4-8` explicitly select the
 provider. The suffix is parsed from the last colon only after trying the whole
 string as an exact model ID, so IDs that contain colons continue to work.
 Supported canonical levels are `none`, `minimal`, `low`, `medium`, `high`,
 `xhigh`, and `max` (subject to the selected model's offered levels). `max` is
-a real level above `xhigh` on models that offer it (currently the Codex
-gpt-5.6 family); on other models it downgrades to `xhigh`, matching its old
-alias behavior.
+a real level above `xhigh` on models that offer it (the Codex gpt-5.6 family
+and the Claude catalog); on other models it downgrades to `xhigh`, matching
+its old alias behavior.
 
 Aliases: `/m`
 
diff --git a/crates/codegen/xai-grok-pager/docs/user-guide/11-custom-models.md b/crates/codegen/xai-grok-pager/docs/user-guide/11-custom-models.md
index 4ec9bea..c622662 100644
--- a/crates/codegen/xai-grok-pager/docs/user-guide/11-custom-models.md
+++ b/crates/codegen/xai-grok-pager/docs/user-guide/11-custom-models.md
@@ -32,6 +32,22 @@ backend; the optional `:effort` suffix chooses reasoning effort (`low` through
 `xhigh`; the gpt-5.6 family also offers `max` above `xhigh`). See
 [Authentication](02-authentication.md#openai-codex-with-a-chatgpt-plan).
 
+## Anthropic Claude built-ins (zero config)
+
+After `grok anthropic login`, Grok automatically adds the built-in
+`anthropic/*` catalog (the adaptive-thinking Claude generation: Opus 4.6–4.8,
+Sonnet 4.6/5, Fable 5). No `[model.*]` configuration is needed:
+
+```
+/model anthropic/claude-opus-4-8:high
+/model anthropic/claude-sonnet-5:max
+```
+
+The provider prefix chooses the Anthropic OAuth credential and Messages
+backend; the optional `:effort` suffix chooses the adaptive thinking effort
+(`low` through `max`; `xhigh` is accepted as an alias for `max`). See
+[Authentication](02-authentication.md#anthropic-claude-with-a-promax-plan).
+
 ## Selecting a Model
 
 ### CLI Flag
@@ -98,7 +114,7 @@ description = "Model description"          # Optional description
 api_key = "sk-..."                        # API key for this provider (optional)
 env_key = "XAI_API_KEY"                   # Env var holding the API key (optional; string or array)
 api_backend = "chat_completions"          # "chat_completions", "responses", or "messages"
-auth_scheme = "bearer"                    # "bearer", "x_api_key", or "chatgpt_oauth"
+auth_scheme = "bearer"                    # "bearer", "x_api_key", "chatgpt_oauth", or "anthropic_oauth"
 temperature = 0.7                         # Sampling temperature
 top_p = 0.95                              # Nucleus sampling parameter
 max_completion_tokens = 8192              # Maximum tokens per response
@@ -191,7 +207,15 @@ context_window = 200000
 extra_headers = { "x-api-key" = "sk-ant-...", "anthropic-version" = "2023-06-01" }
 ```
 
-The `messages` backend uses the Anthropic Messages protocol. Anthropic authenticates with an `x-api-key` header rather than `Authorization: Bearer`, so pass your key through `extra_headers`, which Grok sends verbatim.
+The `messages` backend uses the Anthropic Messages protocol. With an API key, Anthropic authenticates with an `x-api-key` header rather than `Authorization: Bearer`, so pass your key through `extra_headers`, which Grok sends verbatim.
+
+For Claude Pro/Max OAuth, do **not** configure a key manually; use the
+built-in `anthropic/*` catalog described above. Advanced custom entries can
+opt into the stored Anthropic credential with
+`auth_scheme = "anthropic_oauth"`, `api_backend = "messages"`, and
+`base_url = "https://api.anthropic.com/v1"`; Grok then injects the Claude
+Code identity headers and system block that the consumer-OAuth backend
+requires.
 
 ### OpenAI (Chat Completions)
 
-- 
2.50.1 (Apple Git-155)

