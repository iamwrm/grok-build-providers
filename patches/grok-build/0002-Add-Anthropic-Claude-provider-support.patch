From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: grok-build-providers-verify <verify@invalid>
Date: Tue, 1 Sep 2026 10:29:36 +0000
Subject: [PATCH 2/9] Add Anthropic Claude provider support

Add Claude Pro/Max PKCE OAuth with isolated rotating credential storage and grok anthropic login/logout/status commands. Wire anthropic_oauth into the Messages transport with Claude Code identity headers, the required identity system block, consumer-OAuth request shaping, and extended-thinking-safe sampling parameters.
---
 crates/codegen/xai-grok-pager-bin/src/main.rs |  28 +
 crates/codegen/xai-grok-pager/src/app/cli.rs  |  23 +
 crates/codegen/xai-grok-sampler/src/client.rs | 164 ++++-
 crates/codegen/xai-grok-sampler/src/config.rs |   9 +
 .../xai-grok-shell/src/agent/config.rs        |  56 +-
 .../xai-grok-shell/src/auth/anthropic.rs      | 585 ++++++++++++++++++
 crates/codegen/xai-grok-shell/src/auth/mod.rs |   1 +
 .../session/acp_session_impl/sampler_turn.rs  |   7 +
 8 files changed, 865 insertions(+), 8 deletions(-)
 create mode 100644 crates/codegen/xai-grok-shell/src/auth/anthropic.rs

diff --git a/crates/codegen/xai-grok-pager-bin/src/main.rs b/crates/codegen/xai-grok-pager-bin/src/main.rs
index 37304995..6ce11712 100644
--- a/crates/codegen/xai-grok-pager-bin/src/main.rs
+++ b/crates/codegen/xai-grok-pager-bin/src/main.rs
@@ -64,6 +64,17 @@ fn process_identity(command: Option<&Command>, is_interactive: bool) -> Option<P
                 Interactivity::Unattended
             },
         ),
+        Some(Command::Anthropic(args)) => (
+            Entrypoint::Cli,
+            if matches!(
+                &args.command,
+                xai_grok_pager::app::cli::AnthropicCommand::Login
+            ) {
+                Interactivity::Interactive
+            } else {
+                Interactivity::Unattended
+            },
+        ),
         Some(
             Command::Inspect { .. }
             | Command::Doctor(_)
@@ -111,6 +122,7 @@ fn command_needs_pre_sandbox_policy_heal(command: Option<&Command>) -> bool {
             | Command::Logout
             | Command::Login { .. }
             | Command::Openai(_)
+            | Command::Anthropic(_)
             | Command::Mcp(_)
             | Command::Plugin(_)
             | Command::Memory(_)
@@ -2290,6 +2302,22 @@ async fn async_main(args: PagerArgs) -> Result<()> {
                 }
                 xai_grok_shell::instrumentation::finalize_and_exit(0);
             }
+            Command::Anthropic(ref anthropic_args) => {
+                init_tracing_simple("cli");
+                use xai_grok_pager::app::cli::AnthropicCommand;
+                match anthropic_args.command {
+                    AnthropicCommand::Login => {
+                        xai_grok_shell::auth::anthropic::run_cli_login().await?;
+                    }
+                    AnthropicCommand::Logout => {
+                        xai_grok_shell::auth::anthropic::run_cli_logout()?;
+                    }
+                    AnthropicCommand::Status => {
+                        xai_grok_shell::auth::anthropic::run_cli_status()?;
+                    }
+                }
+                xai_grok_shell::instrumentation::finalize_and_exit(0);
+            }
             Command::Logout => {
                 init_tracing_simple("cli");
                 let config = xai_grok_shell::config::load_agent_config_disk_only()
diff --git a/crates/codegen/xai-grok-pager/src/app/cli.rs b/crates/codegen/xai-grok-pager/src/app/cli.rs
index 657e4218..f0a49458 100644
--- a/crates/codegen/xai-grok-pager/src/app/cli.rs
+++ b/crates/codegen/xai-grok-pager/src/app/cli.rs
@@ -45,6 +45,8 @@ pub enum Command {
     },
     /// Manage the OpenAI (ChatGPT) provider credential
     Openai(OpenaiArgs),
+    /// Manage the Anthropic (Claude Pro/Max) provider credential
+    Anthropic(AnthropicArgs),
     /// Manage MCP server configurations
     Mcp(crate::mcp_cmd::McpArgs),
     /// Manage plugins and marketplace sources
@@ -172,6 +174,27 @@ pub enum OpenaiCommand {
     Status,
 }
 
+/// Arguments for the `anthropic` subcommand.
+#[derive(Debug, clap::Args, Clone)]
+pub struct AnthropicArgs {
+    #[command(subcommand)]
+    pub command: AnthropicCommand,
+}
+
+/// `grok anthropic <login|logout|status>` — sign in to Anthropic with a
+/// Claude Pro/Max plan so the `anthropic/*` models become available.
+#[derive(Debug, Subcommand, Clone)]
+pub enum AnthropicCommand {
+    /// Sign in to Anthropic (Claude Pro/Max). Opens a browser; paste the
+    /// redirect URL or the code shown by claude.ai when the browser runs
+    /// on another machine.
+    Login,
+    /// Remove the stored Anthropic credential.
+    Logout,
+    /// Show the stored Anthropic credential status.
+    Status,
+}
+
 /// Arguments for the `wrap` subcommand: the command to run, then its args.
 #[derive(Debug, clap::Args, Clone)]
 pub struct WrapArgs {
diff --git a/crates/codegen/xai-grok-sampler/src/client.rs b/crates/codegen/xai-grok-sampler/src/client.rs
index e8218a5c..43e579d2 100644
--- a/crates/codegen/xai-grok-sampler/src/client.rs
+++ b/crates/codegen/xai-grok-sampler/src/client.rs
@@ -551,6 +551,83 @@ fn apply_chatgpt_codex_request_shape(body: &mut serde_json::Value) {
     obj.remove("top_p");
 }
 
+/// Claude Code identity block required as the first system block on
+/// Anthropic consumer-OAuth (Claude Pro/Max) requests.
+const CLAUDE_CODE_IDENTITY: &str = "You are Claude Code, Anthropic's official CLI for Claude.";
+
+/// System-prompt fragment rewrites for Anthropic OAuth requests.
+///
+/// Anthropic's consumer-OAuth backend runs a server-side classifier that
+/// rejects system prompts of known third-party coding agents with HTTP 400
+/// ("Third-party apps now draw from your extra usage, not your plan
+/// limits..."). The classifier keys on specific text fragments, not prompt
+/// length or structure. Each entry rewrites the minimal trigger fragment
+/// found by binary-search isolation while preserving the instruction's
+/// intent. Anchors are deliberately short so unrelated wording drift cannot
+/// silently disable a replacement.
+///
+/// Currently empty: no grok-build system-prompt fragment is known to
+/// trigger the classifier. When OAuth requests start failing with the
+/// message above, isolate the trigger per prompt section, add the smallest
+/// passing rewrite here, and document it.
+const ANTHROPIC_OAUTH_SYSTEM_REPLACEMENTS: &[(&str, &str)] = &[];
+
+/// Rewrite a typed Messages request for the Anthropic consumer-OAuth
+/// backend, used by models with [`AuthScheme::AnthropicOauth`]:
+///
+/// * the Claude Code identity block is required as the first system block
+///   (the backend rejects third-party system prompts without it);
+/// * [`ANTHROPIC_OAUTH_SYSTEM_REPLACEMENTS`] is applied to the remaining
+///   system text (classifier workaround);
+/// * sampling params (`temperature`, `top_p`) are stripped when extended
+///   thinking is enabled — the API rejects that combination (and Opus 4.7+
+///   rejects `temperature` outright).
+fn apply_anthropic_oauth_request_shape(request: &mut messages::MessagesRequest) {
+    use messages::{CacheControl, SystemParam, TextBlock, ThinkingConfig};
+
+    let mut blocks: Vec<TextBlock> = match request.system.take() {
+        None => Vec::new(),
+        Some(SystemParam::Text(text)) => vec![TextBlock {
+            r#type: "text".to_string(),
+            text,
+            cache_control: None,
+        }],
+        Some(SystemParam::Blocks(blocks)) => blocks,
+    };
+    for block in &mut blocks {
+        for (from, to) in ANTHROPIC_OAUTH_SYSTEM_REPLACEMENTS {
+            if block.text.contains(from) {
+                block.text = block.text.replace(from, to);
+            }
+        }
+    }
+    if blocks.first().map(|b| b.text.as_str()) != Some(CLAUDE_CODE_IDENTITY) {
+        blocks.insert(
+            0,
+            TextBlock {
+                r#type: "text".to_string(),
+                text: CLAUDE_CODE_IDENTITY.to_string(),
+                cache_control: None,
+            },
+        );
+    }
+    // Keep existing cache markers; when none exist (e.g. no system prompt
+    // at all), cache the trailing block so the identity still prefix-caches.
+    if blocks.iter().all(|b| b.cache_control.is_none())
+        && let Some(last) = blocks.last_mut()
+    {
+        last.cache_control = Some(CacheControl {
+            r#type: "ephemeral".to_string(),
+        });
+    }
+    request.system = Some(SystemParam::Blocks(blocks));
+
+    if !matches!(request.thinking, None | Some(ThinkingConfig::Disabled)) {
+        request.temperature = None;
+        request.top_p = None;
+    }
+}
+
 /// Convert a prior assistant easy-input message into the completed output
 /// message shape used by pi/the Codex client:
 ///
@@ -710,7 +787,7 @@ impl SamplingClient {
                     })?;
                     headers.insert(HeaderName::from_static("x-api-key"), header_value);
                 }
-                AuthScheme::Bearer | AuthScheme::ChatgptOauth => {
+                AuthScheme::Bearer | AuthScheme::ChatgptOauth | AuthScheme::AnthropicOauth => {
                     let bearer = format!("Bearer {}", api_key);
                     let header_value = HeaderValue::from_str(&bearer).map_err(|_| {
                         tracing::debug!(
@@ -782,7 +859,9 @@ impl SamplingClient {
         }
 
         // Always set User-Agent: per-session origin if available, else fallback.
-        {
+        // Explicit `extra_headers` win (e.g. the Claude Code `claude-cli/...`
+        // user-agent injected for Anthropic OAuth models).
+        if !headers.contains_key(USER_AGENT) {
             let ua_string = match config.origin_client.as_ref() {
                 Some(origin) => user_agent_string_for(origin),
                 None => user_agent_string_for(&OriginClientInfo {
@@ -864,7 +943,7 @@ impl SamplingClient {
                             headers.insert(HeaderName::from_static("x-api-key"), v);
                         }
                     }
-                    AuthScheme::Bearer | AuthScheme::ChatgptOauth => {
+                    AuthScheme::Bearer | AuthScheme::ChatgptOauth | AuthScheme::AnthropicOauth => {
                         if let Ok(v) = HeaderValue::from_str(&format!("Bearer {fresh}")) {
                             headers.insert(AUTHORIZATION, v);
                         }
@@ -912,7 +991,7 @@ impl SamplingClient {
             AuthScheme::XApiKey => headers
                 .get(HeaderName::from_static("x-api-key"))
                 .and_then(|v| v.to_str().ok()),
-            AuthScheme::Bearer | AuthScheme::ChatgptOauth => headers
+            AuthScheme::Bearer | AuthScheme::ChatgptOauth | AuthScheme::AnthropicOauth => headers
                 .get(AUTHORIZATION)
                 .and_then(|v| v.to_str().ok())
                 .and_then(|s| s.strip_prefix("Bearer ")),
@@ -954,7 +1033,10 @@ impl SamplingClient {
         let auth_prefix = self.current_sent_bearer_suffix();
         let auth_type = match (&self.defaults.auth_scheme, &auth_prefix) {
             (AuthScheme::XApiKey, Some(_)) => "x-api-key",
-            (AuthScheme::Bearer | AuthScheme::ChatgptOauth, Some(_)) => "bearer",
+            (
+                AuthScheme::Bearer | AuthScheme::ChatgptOauth | AuthScheme::AnthropicOauth,
+                Some(_),
+            ) => "bearer",
             (_, None) => "none",
         };
         crate::sampling_log::AuthInfo {
@@ -1674,6 +1756,10 @@ impl SamplingClient {
             request.inner.top_p = self.defaults.top_p;
         }
 
+        if self.defaults.auth_scheme == AuthScheme::AnthropicOauth {
+            apply_anthropic_oauth_request_shape(&mut request.inner);
+        }
+
         Ok(())
     }
 
@@ -3335,4 +3421,72 @@ mod tests {
         assert_eq!(body["instructions"], "part one\npart two");
         assert!(body["input"].as_array().unwrap().is_empty());
     }
+
+    /// Anthropic OAuth request shape: the Claude Code identity becomes the
+    /// first system block, existing system text and its cache marker are
+    /// preserved, and sampling params are stripped when thinking is on.
+    #[test]
+    fn anthropic_oauth_shape_prepends_identity_and_strips_sampling_params() {
+        use xai_grok_sampling_types::messages::{
+            CacheControl, MessagesRequest, SystemParam, TextBlock, ThinkingConfig, ThinkingDisplay,
+        };
+
+        let mut request = MessagesRequest {
+            model: "claude-opus-4-8".to_string(),
+            system: Some(SystemParam::Blocks(vec![TextBlock {
+                r#type: "text".to_string(),
+                text: "You are Grok.".to_string(),
+                cache_control: Some(CacheControl {
+                    r#type: "ephemeral".to_string(),
+                }),
+            }])),
+            temperature: Some(0.7),
+            top_p: Some(0.9),
+            thinking: Some(ThinkingConfig::Adaptive {
+                display: Some(ThinkingDisplay::Summarized),
+            }),
+            ..Default::default()
+        };
+        apply_anthropic_oauth_request_shape(&mut request);
+
+        let Some(SystemParam::Blocks(blocks)) = &request.system else {
+            panic!("system must be in blocks form");
+        };
+        assert_eq!(blocks.len(), 2);
+        assert_eq!(blocks[0].text, CLAUDE_CODE_IDENTITY);
+        assert!(blocks[0].cache_control.is_none());
+        assert_eq!(blocks[1].text, "You are Grok.");
+        assert!(blocks[1].cache_control.is_some(), "cache marker preserved");
+        assert!(request.temperature.is_none());
+        assert!(request.top_p.is_none());
+    }
+
+    /// No system prompt at all → the identity block is still injected (and
+    /// cached); sampling params survive when thinking is disabled.
+    #[test]
+    fn anthropic_oauth_shape_injects_identity_without_system_prompt() {
+        use xai_grok_sampling_types::messages::{MessagesRequest, SystemParam};
+
+        let mut request = MessagesRequest {
+            model: "claude-sonnet-5".to_string(),
+            temperature: Some(0.5),
+            ..Default::default()
+        };
+        apply_anthropic_oauth_request_shape(&mut request);
+
+        let Some(SystemParam::Blocks(blocks)) = &request.system else {
+            panic!("system must be in blocks form");
+        };
+        assert_eq!(blocks.len(), 1);
+        assert_eq!(blocks[0].text, CLAUDE_CODE_IDENTITY);
+        assert!(blocks[0].cache_control.is_some(), "identity prefix-caches");
+        assert_eq!(request.temperature, Some(0.5), "no thinking → params kept");
+
+        // Idempotent: a second application must not duplicate the identity.
+        apply_anthropic_oauth_request_shape(&mut request);
+        let Some(SystemParam::Blocks(blocks)) = &request.system else {
+            panic!("system must be in blocks form");
+        };
+        assert_eq!(blocks.len(), 1);
+    }
 }
diff --git a/crates/codegen/xai-grok-sampler/src/config.rs b/crates/codegen/xai-grok-sampler/src/config.rs
index e68191e2..0ba03af1 100644
--- a/crates/codegen/xai-grok-sampler/src/config.rs
+++ b/crates/codegen/xai-grok-sampler/src/config.rs
@@ -24,6 +24,15 @@ pub enum AuthScheme {
     /// `~/.grok/openai_auth.json` (with refresh) and the ChatGPT Codex
     /// backend headers (`chatgpt-account-id`, `originator`, `OpenAI-Beta`).
     ChatgptOauth,
+    /// Bearer sourced from the Anthropic (Claude Pro/Max) OAuth credential.
+    ///
+    /// Wire format is identical to [`AuthScheme::Bearer`]; the shell layer
+    /// additionally installs a live bearer resolver backed by
+    /// `~/.grok/anthropic_auth.json` (with refresh) and the Claude Code
+    /// identity headers (`anthropic-beta`, `user-agent`, `x-app`), while the
+    /// sampler applies the Claude Code identity system block to Messages
+    /// requests (`apply_anthropic_oauth_request_shape`).
+    AnthropicOauth,
 }
 
 /// All knobs that control a single sampling request.
diff --git a/crates/codegen/xai-grok-shell/src/agent/config.rs b/crates/codegen/xai-grok-shell/src/agent/config.rs
index f5ff319c..70e0fa9d 100644
--- a/crates/codegen/xai-grok-shell/src/agent/config.rs
+++ b/crates/codegen/xai-grok-shell/src/agent/config.rs
@@ -4298,6 +4298,9 @@ impl ModelEntry {
         if self.info.auth_scheme == AuthScheme::ChatgptOauth {
             return crate::auth::openai_codex::current_bearer_blocking();
         }
+        if self.info.auth_scheme == AuthScheme::AnthropicOauth {
+            return crate::auth::anthropic::current_bearer_blocking();
+        }
         first_own_credential(self.api_key.as_deref(), self.env_key.as_ref())
     }
     /// The provider governing this model's bearer: `None` when a static `api_key`/`env_key` resolves.
@@ -4703,6 +4706,19 @@ pub(crate) fn resolve_credentials(
             );
         }
         (key, info.base_url.clone(), xai_chat_state::AuthType::ApiKey)
+    } else if info.auth_scheme == AuthScheme::AnthropicOauth {
+        // Anthropic-OAuth models likewise never fall back to the xAI session
+        // token or XAI_API_KEY: the Messages backend only accepts the
+        // Anthropic bearer.
+        let key = crate::auth::anthropic::current_bearer_blocking();
+        if key.is_none() {
+            tracing::warn!(
+                model = %info.model,
+                "model uses Anthropic OAuth but no credential is stored \u{2014} \
+                 run `grok anthropic login`",
+            );
+        }
+        (key, info.base_url.clone(), xai_chat_state::AuthType::ApiKey)
     } else if let Some(key) = model.own_credential() {
         (
             Some(key),
@@ -4862,10 +4878,15 @@ pub(crate) fn resolve_model_auth_facts_and_provider(
 fn byok_from_lookup(lookup: &ModelLookup) -> ModelByok {
     match lookup {
         ModelLookup::ConfigUnavailable => ModelByok::Unknown,
-        // ChatGPT-OAuth models are always BYOK: even when the user is not
-        // signed in to OpenAI, the xAI session token must never be attached.
+        // Provider-OAuth models are always BYOK: even when the user is not
+        // signed in to OpenAI/Anthropic, the xAI session token must never
+        // be attached.
         ModelLookup::Loaded(Some(e))
-            if e.has_own_credentials() || e.info().auth_scheme == AuthScheme::ChatgptOauth =>
+            if e.has_own_credentials()
+                || matches!(
+                    e.info().auth_scheme,
+                    AuthScheme::ChatgptOauth | AuthScheme::AnthropicOauth
+                ) =>
         {
             ModelByok::Byok
         }
@@ -5097,6 +5118,11 @@ pub(crate) fn sampling_config_for_model(
             Some(std::sync::Arc::new(
                 crate::auth::openai_codex::OpenAiCodexBearerResolver,
             ))
+        } else if credentials.auth_scheme == AuthScheme::AnthropicOauth {
+            inject_anthropic_oauth_headers(&mut extra_headers);
+            Some(std::sync::Arc::new(
+                crate::auth::anthropic::AnthropicBearerResolver,
+            ))
         } else {
             None
         };
@@ -5163,6 +5189,30 @@ pub fn inject_openai_codex_headers(extra_headers: &mut IndexMap<String, String>)
         .entry("OpenAI-Beta".to_string())
         .or_insert_with(|| "responses=experimental".to_string());
 }
+/// Fold the Anthropic consumer-OAuth (Claude Code identity) headers into
+/// `extra_headers` for models with `auth_scheme = "anthropic_oauth"`:
+///
+/// * `anthropic-version` — required by the Messages API,
+/// * `anthropic-beta` — opts in to OAuth bearer auth + Claude Code identity,
+/// * `user-agent`/`x-app` — Claude Code client identity (the sampler skips
+///   its own User-Agent when one is provided here).
+///
+/// Explicit user-provided headers win (entries are only inserted when the
+/// key is absent).
+pub fn inject_anthropic_oauth_headers(extra_headers: &mut IndexMap<String, String>) {
+    extra_headers
+        .entry("anthropic-version".to_string())
+        .or_insert_with(|| "2023-06-01".to_string());
+    extra_headers
+        .entry("anthropic-beta".to_string())
+        .or_insert_with(|| crate::auth::anthropic::OAUTH_BETA_HEADER.to_string());
+    extra_headers
+        .entry("user-agent".to_string())
+        .or_insert_with(|| format!("claude-cli/{}", crate::auth::anthropic::CLAUDE_CODE_VERSION));
+    extra_headers
+        .entry("x-app".to_string())
+        .or_insert_with(|| "cli".to_string());
+}
 /// Fold URL-derived headers into `extra_headers`.
 ///
 /// The sampler crate is intentionally URL-agnostic: it does not inspect `base_url` to decide which auth or staging headers to add.
diff --git a/crates/codegen/xai-grok-shell/src/auth/anthropic.rs b/crates/codegen/xai-grok-shell/src/auth/anthropic.rs
new file mode 100644
index 00000000..0a6b363c
--- /dev/null
+++ b/crates/codegen/xai-grok-shell/src/auth/anthropic.rs
@@ -0,0 +1,585 @@
+//! Anthropic (Claude Pro/Max) OAuth.
+//!
+//! Reimplements the consumer OAuth flow used by Claude Code (and by pi's
+//! `anthropic` provider): PKCE authorization-code flow against
+//! `claude.ai` with a fixed loopback callback on port 53692, plus a
+//! paste-the-code fallback for browsers on another machine (claude.ai
+//! displays a `code#state` pair for manual copy).
+//!
+//! The resulting access token (`sk-ant-oat...`) is used as the bearer for
+//! requests to the Anthropic Messages API together with the Claude Code
+//! identity headers (`anthropic-beta: claude-code-...,oauth-...`).
+//!
+//! Credentials are stored in `~/.grok/anthropic_auth.json` (owner-only),
+//! separate from the xAI session in `auth.json`: this is a per-provider
+//! model credential, not the Grok session.
+
+use std::collections::HashMap;
+use std::io::IsTerminal;
+use std::path::PathBuf;
+use std::sync::Mutex;
+
+use anyhow::{Context, anyhow, bail};
+use axum::{
+    Router,
+    extract::{Query, State},
+    http::StatusCode,
+    response::Html,
+    routing::get,
+};
+use base64::Engine;
+use base64::engine::general_purpose::URL_SAFE_NO_PAD;
+use chrono::Utc;
+use serde::{Deserialize, Serialize};
+use sha2::{Digest, Sha256};
+use tokio::net::TcpListener;
+
+/// Public client id of the official Claude Code CLI (same one pi uses).
+const CLIENT_ID: &str = "9d1c250a-e61b-44d9-88ed-5944d1962f5e";
+const AUTHORIZE_URL: &str = "https://claude.ai/oauth/authorize";
+const TOKEN_URL: &str = "https://platform.claude.com/v1/oauth/token";
+/// Fixed loopback callback registered for [`CLIENT_ID`]; the port must be 53692.
+const REDIRECT_URI: &str = "http://localhost:53692/callback";
+const CALLBACK_PORT: u16 = 53692;
+const SCOPES: &str = "org:create_api_key user:profile user:inference \
+                      user:sessions:claude_code user:mcp_servers user:file_upload";
+/// Claude Code CLI version advertised in the `user-agent` header
+/// (`claude-cli/<version>`); kept in sync with pi's `anthropic-messages`.
+pub const CLAUDE_CODE_VERSION: &str = "2.1.75";
+/// `anthropic-beta` value required for OAuth (Claude Code identity) requests.
+pub const OAUTH_BETA_HEADER: &str = "claude-code-20250219,oauth-2025-04-20";
+/// Refresh this long before expiry.
+const EARLY_REFRESH_SECS: i64 = 300;
+/// Maximum time to wait for the browser callback / pasted code.
+const AUTH_CALLBACK_TIMEOUT: std::time::Duration = std::time::Duration::from_secs(600);
+
+// ============================================================================
+// Credential storage
+// ============================================================================
+
+/// Stored Anthropic OAuth credential.
+#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
+pub struct AnthropicCredential {
+    pub access_token: String,
+    pub refresh_token: String,
+    /// Unix seconds when `access_token` expires.
+    pub expires_at: i64,
+}
+
+impl AnthropicCredential {
+    pub fn is_expired_or_near(&self) -> bool {
+        self.expires_at - EARLY_REFRESH_SECS <= Utc::now().timestamp()
+    }
+}
+
+/// Path of the stored credential: `~/.grok/anthropic_auth.json`.
+pub fn credential_path() -> PathBuf {
+    crate::util::grok_home::grok_home().join("anthropic_auth.json")
+}
+
+/// Read the stored credential, if any. Corrupt files read as `None`.
+pub fn read_credential() -> Option<AnthropicCredential> {
+    let raw = std::fs::read_to_string(credential_path()).ok()?;
+    match serde_json::from_str(&raw) {
+        Ok(cred) => Some(cred),
+        Err(e) => {
+            tracing::warn!(error = %e, "anthropic auth: failed to parse anthropic_auth.json");
+            None
+        }
+    }
+}
+
+/// Persist the credential with owner-only permissions.
+pub fn store_credential(cred: &AnthropicCredential) -> anyhow::Result<()> {
+    let path = credential_path();
+    if let Some(parent) = path.parent() {
+        std::fs::create_dir_all(parent)
+            .with_context(|| format!("creating {}", parent.display()))?;
+    }
+    let json = serde_json::to_string_pretty(cred)?;
+    std::fs::write(&path, json).with_context(|| format!("writing {}", path.display()))?;
+    #[cfg(unix)]
+    {
+        use std::os::unix::fs::PermissionsExt;
+        let _ = std::fs::set_permissions(&path, std::fs::Permissions::from_mode(0o600));
+    }
+    Ok(())
+}
+
+/// Delete the stored credential. Returns `true` if a file was removed.
+pub fn clear_credential() -> anyhow::Result<bool> {
+    match std::fs::remove_file(credential_path()) {
+        Ok(()) => Ok(true),
+        Err(e) if e.kind() == std::io::ErrorKind::NotFound => Ok(false),
+        Err(e) => Err(e.into()),
+    }
+}
+
+// ============================================================================
+// Token plumbing
+// ============================================================================
+
+#[derive(Debug, Deserialize)]
+struct TokenResponse {
+    access_token: String,
+    refresh_token: String,
+    expires_in: i64,
+}
+
+fn credential_from_token_response(tokens: TokenResponse) -> AnthropicCredential {
+    AnthropicCredential {
+        access_token: tokens.access_token,
+        refresh_token: tokens.refresh_token,
+        expires_at: Utc::now().timestamp() + tokens.expires_in,
+    }
+}
+
+async fn read_token_response(
+    response: reqwest::Response,
+    operation: &str,
+) -> anyhow::Result<TokenResponse> {
+    let status = response.status();
+    let body = response.text().await.unwrap_or_default();
+    if !status.is_success() {
+        bail!("Anthropic token {operation} failed ({status}): {body}");
+    }
+    serde_json::from_str(&body)
+        .with_context(|| format!("Anthropic token {operation} response missing fields: {body}"))
+}
+
+/// Exchange the authorization code. Unlike the OpenAI endpoint this one
+/// takes a JSON body and additionally requires the `state` value.
+async fn exchange_authorization_code(
+    code: &str,
+    state: &str,
+    code_verifier: &str,
+) -> anyhow::Result<AnthropicCredential> {
+    let response = reqwest::Client::new()
+        .post(TOKEN_URL)
+        .json(&serde_json::json!({
+            "grant_type": "authorization_code",
+            "client_id": CLIENT_ID,
+            "code": code,
+            "state": state,
+            "redirect_uri": REDIRECT_URI,
+            "code_verifier": code_verifier,
+        }))
+        .send()
+        .await
+        .context("Anthropic token exchange request failed")?;
+    Ok(credential_from_token_response(
+        read_token_response(response, "exchange").await?,
+    ))
+}
+
+/// Refresh the access token with the stored refresh token.
+pub async fn refresh_credential(refresh_token: &str) -> anyhow::Result<AnthropicCredential> {
+    let response = reqwest::Client::new()
+        .post(TOKEN_URL)
+        .json(&serde_json::json!({
+            "grant_type": "refresh_token",
+            "client_id": CLIENT_ID,
+            "refresh_token": refresh_token,
+        }))
+        .send()
+        .await
+        .context("Anthropic token refresh request failed")?;
+    let cred = credential_from_token_response(read_token_response(response, "refresh").await?);
+    store_credential(&cred)?;
+    Ok(cred)
+}
+
+/// Read the stored credential, refreshing (and persisting) when near expiry.
+pub async fn ensure_fresh_credential() -> anyhow::Result<AnthropicCredential> {
+    let cred = read_credential()
+        .ok_or_else(|| anyhow!("not signed in to Anthropic. Run `grok anthropic login` first"))?;
+    if !cred.is_expired_or_near() {
+        return Ok(cred);
+    }
+    tracing::info!("anthropic auth: access token near expiry, refreshing");
+    refresh_credential(&cred.refresh_token).await
+}
+
+/// Live bearer source for the sampler: reads (and refreshes) the stored
+/// Anthropic OAuth credential on every request. Installed by the shell for
+/// models with `auth_scheme = "anthropic_oauth"`.
+#[derive(Debug)]
+pub struct AnthropicBearerResolver;
+
+impl xai_grok_sampler::BearerResolver for AnthropicBearerResolver {
+    fn current_bearer(&self) -> Option<String> {
+        current_bearer_blocking()
+    }
+}
+
+/// Process-wide single-flight guard for [`current_bearer_blocking`] refreshes.
+static REFRESH_LOCK: Mutex<()> = Mutex::new(());
+
+/// Synchronous bearer lookup for the sampler's `BearerResolver` seam.
+///
+/// Reads the credential from disk; when near expiry, performs a blocking
+/// refresh on a dedicated thread (safe to call from async contexts — the
+/// HTTP call runs on its own thread with its own runtime).
+pub fn current_bearer_blocking() -> Option<String> {
+    let cred = read_credential()?;
+    if !cred.is_expired_or_near() {
+        return Some(cred.access_token);
+    }
+    let _guard = REFRESH_LOCK.lock().ok()?;
+    // Re-read: another thread may have refreshed while we waited on the lock.
+    let cred = read_credential()?;
+    if !cred.is_expired_or_near() {
+        return Some(cred.access_token);
+    }
+    let refresh_token = cred.refresh_token.clone();
+    let result = std::thread::spawn(move || {
+        tokio::runtime::Builder::new_current_thread()
+            .enable_all()
+            .build()
+            .map_err(anyhow::Error::from)
+            .and_then(|rt| rt.block_on(refresh_credential(&refresh_token)))
+    })
+    .join();
+    match result {
+        Ok(Ok(fresh)) => Some(fresh.access_token),
+        Ok(Err(e)) => {
+            tracing::warn!(error = %e, "anthropic auth: token refresh failed");
+            // Serve the stale token; the request will 401 with a clear error.
+            Some(cred.access_token)
+        }
+        Err(_) => Some(cred.access_token),
+    }
+}
+
+// ============================================================================
+// PKCE
+// ============================================================================
+
+struct Pkce {
+    verifier: String,
+    challenge: String,
+}
+
+fn generate_pkce() -> Pkce {
+    use rand::RngCore;
+    let mut random_bytes = [0u8; 32];
+    rand::rng().fill_bytes(&mut random_bytes);
+    let verifier = URL_SAFE_NO_PAD.encode(random_bytes);
+    let challenge = URL_SAFE_NO_PAD.encode(Sha256::digest(verifier.as_bytes()));
+    Pkce {
+        verifier,
+        challenge,
+    }
+}
+
+/// The Claude flow uses the PKCE verifier itself as the OAuth `state`
+/// (pi parity), so the pasted `code#state` fallback can be validated
+/// without any extra bookkeeping.
+fn build_authorize_url(pkce: &Pkce) -> String {
+    let mut url = url::Url::parse(AUTHORIZE_URL).expect("static authorize URL parses");
+    url.query_pairs_mut()
+        .append_pair("code", "true")
+        .append_pair("client_id", CLIENT_ID)
+        .append_pair("response_type", "code")
+        .append_pair("redirect_uri", REDIRECT_URI)
+        .append_pair("scope", SCOPES)
+        .append_pair("code_challenge", &pkce.challenge)
+        .append_pair("code_challenge_method", "S256")
+        .append_pair("state", &pkce.verifier);
+    url.to_string()
+}
+
+// ============================================================================
+// Browser login (loopback callback on port 53692 + stdin paste fallback)
+// ============================================================================
+
+#[derive(Debug)]
+struct Callback {
+    code: String,
+    state: String,
+}
+
+type CallbackResult = Result<Callback, String>;
+
+/// Parse a pasted redirect URL, `code#state` pair, or bare code.
+/// claude.ai shows the manual-copy credential as `code#state`.
+fn parse_pasted_input(input: &str) -> Option<Callback> {
+    let input = input.trim();
+    if input.is_empty() {
+        return None;
+    }
+    if let Ok(url) = url::Url::parse(input) {
+        let params: HashMap<String, String> = url.query_pairs().into_owned().collect();
+        if let Some(code) = params.get("code") {
+            return Some(Callback {
+                code: code.clone(),
+                state: params.get("state").cloned().unwrap_or_default(),
+            });
+        }
+        return None;
+    }
+    if let Some((code, state)) = input.split_once('#') {
+        return Some(Callback {
+            code: code.to_owned(),
+            state: state.to_owned(),
+        });
+    }
+    Some(Callback {
+        code: input.to_owned(),
+        state: String::new(),
+    })
+}
+
+fn callback_page(title: &str, message: &str) -> String {
+    format!(
+        r#"<!DOCTYPE html>
+<html lang="en"><head><meta charset="utf-8"/><meta name="color-scheme" content="light dark"/>
+<title>{title}</title>
+<style>body{{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
+display:flex;align-items:center;justify-content:center;min-height:100vh;background:#0a0a0a;color:#e5e5e5}}
+.card{{text-align:center;padding:48px}}h1{{font-size:18px;font-weight:600}}p{{font-size:14px;color:#a3a3a3}}
+@media(prefers-color-scheme:light){{body{{background:#fafafa;color:#171717}}p{{color:#525252}}}}</style>
+</head><body><div class="card"><h1>{title}</h1><p>{message}</p></div></body></html>"#
+    )
+}
+
+async fn handle_callback(
+    State(tx): State<tokio::sync::mpsc::Sender<CallbackResult>>,
+    Query(params): Query<HashMap<String, String>>,
+) -> (StatusCode, Html<String>) {
+    let result = if let Some(code) = params.get("code") {
+        Ok(Callback {
+            code: code.clone(),
+            state: params.get("state").cloned().unwrap_or_default(),
+        })
+    } else {
+        let error = params.get("error").cloned().unwrap_or_default();
+        let desc = params.get("error_description").cloned().unwrap_or_default();
+        Err(if desc.is_empty() {
+            error
+        } else {
+            format!("{error}: {desc}")
+        })
+    };
+    let response = match &result {
+        Ok(_) => (
+            StatusCode::OK,
+            Html(callback_page(
+                "Signed in to Anthropic",
+                "You can close this window and return to Grok Build.",
+            )),
+        ),
+        Err(_) => (
+            StatusCode::OK,
+            Html(callback_page(
+                "Sign-in failed",
+                "Close this window and try again.",
+            )),
+        ),
+    };
+    if let Err(e) = tx.try_send(result) {
+        tracing::error!(?e, "anthropic auth: callback channel send failed");
+    }
+    response
+}
+
+fn spawn_stdin_reader(tx: tokio::sync::mpsc::Sender<CallbackResult>) {
+    tokio::task::spawn_blocking(move || {
+        use std::io::BufRead;
+        let stdin = std::io::stdin();
+        loop {
+            if tx.is_closed() {
+                return;
+            }
+            let mut buf = String::new();
+            match stdin.lock().read_line(&mut buf) {
+                Ok(0) | Err(_) => return,
+                Ok(_) => {}
+            }
+            let trimmed = buf.trim();
+            if trimmed.is_empty() {
+                continue;
+            }
+            match parse_pasted_input(trimmed) {
+                Some(cb) => {
+                    let _ = tx.blocking_send(Ok(cb));
+                    return;
+                }
+                None => eprintln!(
+                    "  Could not parse input; paste the full redirect URL or the code#state pair:"
+                ),
+            }
+        }
+    });
+}
+
+async fn login_browser() -> anyhow::Result<AnthropicCredential> {
+    let pkce = generate_pkce();
+    let auth_url = build_authorize_url(&pkce);
+
+    let listener = TcpListener::bind(("127.0.0.1", CALLBACK_PORT))
+        .await
+        .map_err(|e| {
+            anyhow!(
+                "could not bind 127.0.0.1:{CALLBACK_PORT} for the Anthropic login callback \
+                 ({e}). Close the conflicting program and retry."
+            )
+        })?;
+
+    let (tx, mut rx) = tokio::sync::mpsc::channel::<CallbackResult>(1);
+    let (shutdown_tx, shutdown_rx) = tokio::sync::oneshot::channel::<()>();
+    let app = Router::new()
+        .route("/callback", get(handle_callback))
+        .with_state(tx.clone());
+    let server = tokio::spawn(async move {
+        let _ = axum::serve(listener, app)
+            .with_graceful_shutdown(async {
+                let _ = shutdown_rx.await;
+            })
+            .await;
+    });
+
+    eprintln!();
+    eprintln!("Signing in to Anthropic (Claude Pro/Max)...");
+    eprintln!();
+    if let Err(e) = webbrowser::open(&auth_url) {
+        tracing::debug!(error = %e, "anthropic auth: failed to open browser");
+    }
+    eprintln!("Open this URL to sign in:");
+    eprintln!("  {auth_url}");
+    let use_stdin = std::io::stdin().is_terminal();
+    if use_stdin {
+        eprintln!();
+        eprintln!(
+            "Paste the redirect URL or the code shown by claude.ai here if the browser \
+             can't reach this machine:"
+        );
+        spawn_stdin_reader(tx.clone());
+    }
+    drop(tx);
+
+    let callback = tokio::time::timeout(AUTH_CALLBACK_TIMEOUT, rx.recv())
+        .await
+        .map_err(|_| anyhow!("timed out waiting for the Anthropic login callback"))?
+        .ok_or_else(|| anyhow!("Anthropic login callback channel closed"))?
+        .map_err(|e| anyhow!("Anthropic sign-in failed: {e}"))?;
+
+    let _ = shutdown_tx.send(());
+    let _ = server.await;
+
+    if !callback.state.is_empty() && callback.state != pkce.verifier {
+        bail!("Anthropic sign-in failed: state mismatch");
+    }
+    let state = if callback.state.is_empty() {
+        pkce.verifier.clone()
+    } else {
+        callback.state
+    };
+    exchange_authorization_code(&callback.code, &state, &pkce.verifier).await
+}
+
+// ============================================================================
+// CLI entry points
+// ============================================================================
+
+/// `grok anthropic login`
+pub async fn run_cli_login() -> anyhow::Result<()> {
+    let cred = login_browser().await?;
+    store_credential(&cred)?;
+    eprintln!();
+    eprintln!(
+        "Signed in to Anthropic. Credential saved to {}.",
+        credential_path().display()
+    );
+    eprintln!("Switch models with `/model anthropic/<model>` inside Grok.");
+    Ok(())
+}
+
+/// `grok anthropic logout`
+pub fn run_cli_logout() -> anyhow::Result<()> {
+    if clear_credential()? {
+        eprintln!(
+            "Signed out of Anthropic; removed {}.",
+            credential_path().display()
+        );
+    } else {
+        eprintln!("No Anthropic credential to remove.");
+    }
+    Ok(())
+}
+
+/// `grok anthropic status`
+pub fn run_cli_status() -> anyhow::Result<()> {
+    match read_credential() {
+        Some(cred) => {
+            let expires = chrono::DateTime::from_timestamp(cred.expires_at, 0)
+                .map(|t| t.to_rfc3339())
+                .unwrap_or_else(|| cred.expires_at.to_string());
+            println!("Signed in to Anthropic (Claude Pro/Max).");
+            println!(
+                "  access token: {}expired (expires {expires})",
+                if cred.is_expired_or_near() {
+                    ""
+                } else {
+                    "not "
+                }
+            );
+            println!("  credential file: {}", credential_path().display());
+        }
+        None => {
+            println!("Not signed in to Anthropic. Run `grok anthropic login`.");
+        }
+    }
+    Ok(())
+}
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn parses_pasted_redirect_url_code_hash_state_and_bare_code() {
+        let cb = parse_pasted_input("http://localhost:53692/callback?code=abc&state=xyz").unwrap();
+        assert_eq!((cb.code.as_str(), cb.state.as_str()), ("abc", "xyz"));
+
+        // claude.ai manual-copy format
+        let cb = parse_pasted_input("abc#xyz").unwrap();
+        assert_eq!((cb.code.as_str(), cb.state.as_str()), ("abc", "xyz"));
+
+        let cb = parse_pasted_input("  bare-code  ").unwrap();
+        assert_eq!((cb.code.as_str(), cb.state.as_str()), ("bare-code", ""));
+
+        assert!(parse_pasted_input("").is_none());
+        assert!(parse_pasted_input("http://localhost:53692/callback?error=denied").is_none());
+    }
+
+    #[test]
+    fn authorize_url_carries_pkce_and_uses_verifier_as_state() {
+        let pkce = generate_pkce();
+        let url = build_authorize_url(&pkce);
+        let parsed = url::Url::parse(&url).unwrap();
+        let params: HashMap<String, String> = parsed.query_pairs().into_owned().collect();
+        assert_eq!(params["client_id"], CLIENT_ID);
+        assert_eq!(params["redirect_uri"], REDIRECT_URI);
+        assert_eq!(params["code"], "true");
+        assert_eq!(params["code_challenge_method"], "S256");
+        assert_eq!(params["state"], pkce.verifier);
+        assert_eq!(
+            params["code_challenge"],
+            URL_SAFE_NO_PAD.encode(Sha256::digest(pkce.verifier.as_bytes()))
+        );
+        assert!(params["scope"].contains("user:inference"));
+    }
+
+    #[test]
+    fn expiry_check_honors_early_refresh_window() {
+        let mut cred = AnthropicCredential {
+            access_token: "a".into(),
+            refresh_token: "r".into(),
+            expires_at: Utc::now().timestamp() + 3600,
+        };
+        assert!(!cred.is_expired_or_near());
+        cred.expires_at = Utc::now().timestamp() + EARLY_REFRESH_SECS - 10;
+        assert!(cred.is_expired_or_near());
+    }
+}
diff --git a/crates/codegen/xai-grok-shell/src/auth/mod.rs b/crates/codegen/xai-grok-shell/src/auth/mod.rs
index a01aec4e..a8f0c635 100644
--- a/crates/codegen/xai-grok-shell/src/auth/mod.rs
+++ b/crates/codegen/xai-grok-shell/src/auth/mod.rs
@@ -1,3 +1,4 @@
+pub mod anthropic;
 pub(crate) mod api_key_probe;
 pub(crate) mod attribution;
 mod auth_provider;
diff --git a/crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs b/crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
index bcbf8abf..dc0f30f2 100644
--- a/crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
+++ b/crates/codegen/xai-grok-shell/src/session/acp_session_impl/sampler_turn.rs
@@ -672,8 +672,11 @@ impl SessionActor {
             &cfg.base_url,
         );
         let is_chatgpt_oauth = auth_scheme == xai_grok_sampler::AuthScheme::ChatgptOauth;
+        let is_anthropic_oauth = auth_scheme == xai_grok_sampler::AuthScheme::AnthropicOauth;
         if is_chatgpt_oauth {
             crate::agent::config::inject_openai_codex_headers(&mut extra_headers);
+        } else if is_anthropic_oauth {
+            crate::agent::config::inject_anthropic_oauth_headers(&mut extra_headers);
         }
         let compaction_at_tokens = self.compaction_at_tokens.get();
         let compactions_remaining = self.compactions_remaining.get();
@@ -745,6 +748,10 @@ impl SessionActor {
                 Some(std::sync::Arc::new(
                     crate::auth::openai_codex::OpenAiCodexBearerResolver,
                 ))
+            } else if is_anthropic_oauth {
+                Some(std::sync::Arc::new(
+                    crate::auth::anthropic::AnthropicBearerResolver,
+                ))
             } else if use_bearer_resolver {
                 self.auth_manager.as_ref().map(|am| {
                     crate::auth::credential_provider::WireValidBearerResolver::shared(am.clone())
